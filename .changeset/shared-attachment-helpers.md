---
"helldots": patch
---

The screenshot-attachment pipeline (preview strip, file input with its 5-attachment cap, lightbox wiring) existed copied three to five times across the comment box, thread popover and inbox — and had already drifted once. It is now one set of shared helpers, and the marker size, caret icon, platform check and inbox filter reset each live in one place.
