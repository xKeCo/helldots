---
"helldots": patch
---

Accessibility: the screenshot lightbox declares `aria-modal` and traps Tab on its close button — keyboard focus no longer walks into the page behind the backdrop while the dialog is open.
