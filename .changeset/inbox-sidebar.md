---
"helldots": minor
---

Right-side inbox sidebar with page filter, agent-context copy, delete and
thread detail view; optional `persistence: "localStorage"` mode with
cross-page storage; `user` option for comment authorship; runtime hidden
state for markers whose anchor element is not visible (responsive
display:none); `deleteComment()` and `onCommentDeleted`. Serialized
comments now include `page` and `screenshots`.
