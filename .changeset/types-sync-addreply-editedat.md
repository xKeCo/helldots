---
"helldots": patch
---

Type declarations caught up with the implementation: `addReply` declares its optional `screenshots` parameter (TS hosts no longer need a cast to attach reply screenshots) and `Comment` declares `editedAt`.
