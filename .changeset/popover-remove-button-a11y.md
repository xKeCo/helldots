---
"helldots": patch
---

The thread popover's remove-screenshot button regained `type="button"` and its localized `aria-label`, matching the comment-box and inbox copies of the same preview. Its capture warning also gained the `HellDots:` prefix every other diagnostic carries.
